{
  "type": "object",
  "definitions": {
    "useTabs": {
      "description": "Whether to use tabs (true) or spaces (false).",
      "type": "boolean",
      "oneOf": [
        {
          "description": "",
          "type": "boolean",
          "enum": [
            true
          ]
        },
        {
          "description": "",
          "type": "boolean",
          "enum": [
            false
          ]
        }
      ]
    },
    "newLineKind": {
      "description": "The kind of newline to use.",
      "type": "string",
      "oneOf": [
        {
          "description": "For each file, uses the newline kind found at the end of the last line.",
          "type": "string",
          "enum": [
            "auto"
          ]
        },
        {
          "description": "Uses carriage return, line feed.",
          "type": "string",
          "enum": [
            "crlf"
          ]
        },
        {
          "description": "Uses line feed.",
          "type": "string",
          "enum": [
            "lf"
          ]
        },
        {
          "description": "Uses the system standard (ex. crlf on Windows).",
          "type": "string",
          "enum": [
            "system"
          ]
        }
      ]
    },
    "quoteStyle": {
      "description": "Which quote to use for a single-line string. A string is only rewritten when doing so doesn't require adding an escape, since a basic string and a literal string spell a backslash differently. Keys are left as written.",
      "type": "string",
      "oneOf": [
        {
          "description": "Keeps the quote the string was written with.",
          "type": "string",
          "enum": [
            "maintain"
          ]
        },
        {
          "description": "Uses a double quote when doing so doesn't require adding an escape (ex. `'a'` becomes `\"a\"`).",
          "type": "string",
          "enum": [
            "preferDouble"
          ]
        },
        {
          "description": "Uses a single quote when doing so doesn't require adding an escape (ex. `\"a\"` becomes `'a'`).",
          "type": "string",
          "enum": [
            "preferSingle"
          ]
        }
      ]
    },
    "indentTables": {
      "description": "Whether to indent a table header that is a subtable of the header above it.",
      "type": "string",
      "oneOf": [
        {
          "description": "Indents a table header that was written indented and leaves the rest at the margin.",
          "type": "string",
          "enum": [
            "maintain"
          ]
        },
        {
          "description": "Indents every table header by how many of the headers above it name one of its ancestors.",
          "type": "string",
          "enum": [
            "always"
          ]
        },
        {
          "description": "Writes every table header at the margin.",
          "type": "string",
          "enum": [
            "never"
          ]
        }
      ]
    },
    "indentEntries": {
      "description": "Whether to indent the entries of a table a level in from its header. Entries written above the first table header are never indented.",
      "type": "string",
      "oneOf": [
        {
          "description": "Indents the entries that were written further in than their table header.",
          "type": "string",
          "enum": [
            "maintain"
          ]
        },
        {
          "description": "Indents every table's entries.",
          "type": "string",
          "enum": [
            "always"
          ]
        },
        {
          "description": "Writes a table's entries at the same indent as its header.",
          "type": "string",
          "enum": [
            "never"
          ]
        }
      ]
    },
    "trailingCommas": {
      "description": "When to write a trailing comma after the last value of an array or inline table.",
      "type": "string",
      "oneOf": [
        {
          "description": "Writes one only when the array or inline table is written over multiple lines.",
          "type": "string",
          "enum": [
            "onlyMultiLine"
          ]
        },
        {
          "description": "Never writes one.",
          "type": "string",
          "enum": [
            "never"
          ]
        }
      ]
    },
    "spaceSurroundingEquals": {
      "description": "Whether to write a space on either side of the `=` of a key/value pair.",
      "type": "boolean",
      "oneOf": [
        {
          "description": "ex. `key = 1`",
          "type": "boolean",
          "enum": [
            true
          ]
        },
        {
          "description": "ex. `key=1`",
          "type": "boolean",
          "enum": [
            false
          ]
        }
      ]
    },
    "sortKeys": {
      "description": "Whether to alphabetically sort the entries of each table. Entries separated by a blank line are sorted within their own run, and table headers are never moved. A comment travels with the entry written beneath it, unless a blank line sets it apart -- either above it or between it and the entry -- which marks it as a heading for the run and keeps it at the top.",
      "type": "boolean",
      "oneOf": [
        {
          "description": "",
          "type": "boolean",
          "enum": [
            true
          ]
        },
        {
          "description": "",
          "type": "boolean",
          "enum": [
            false
          ]
        }
      ]
    },
    "sortArrays": {
      "description": "Whether to alphabetically sort the values of an array. Values separated by a blank line are sorted within their own run, unless the array is written on a single line, which leaves nowhere to keep those dividers. An array holding an array or inline table is left alone. A comment travels with the value written beneath it, unless a blank line sets it apart -- either above it or between it and the value -- which marks it as a heading for the run and keeps it at the top. Note that this changes the meaning of a document whose array order is significant.",
      "type": "boolean",
      "oneOf": [
        {
          "description": "",
          "type": "boolean",
          "enum": [
            true
          ]
        },
        {
          "description": "",
          "type": "boolean",
          "enum": [
            false
          ]
        }
      ]
    },
    "sortInlineTables": {
      "description": "Whether to alphabetically sort the entries of an inline table. A comment travels with the entry written beneath it, unless a blank line sets it apart -- either above it or between it and the entry -- which marks it as a heading for the run and keeps it at the top.",
      "type": "boolean",
      "oneOf": [
        {
          "description": "",
          "type": "boolean",
          "enum": [
            true
          ]
        },
        {
          "description": "",
          "type": "boolean",
          "enum": [
            false
          ]
        }
      ]
    },
    "preferSingleLine": {
      "description": "Whether to collapse an array or inline table written over several lines onto a single line when it fits. A comment written within one keeps it expanded.",
      "type": "boolean",
      "oneOf": [
        {
          "description": "",
          "type": "boolean",
          "enum": [
            true
          ]
        },
        {
          "description": "",
          "type": "boolean",
          "enum": [
            false
          ]
        }
      ]
    },
    "array.preferSingleLine": {
      "description": "Whether to collapse an array written over several lines onto a single line when it fits. A comment written within it keeps it expanded. Defaults to whatever `preferSingleLine` is set to.",
      "type": "boolean",
      "oneOf": [
        {
          "description": "",
          "type": "boolean",
          "enum": [
            true
          ]
        },
        {
          "description": "",
          "type": "boolean",
          "enum": [
            false
          ]
        }
      ]
    },
    "inlineTable.preferSingleLine": {
      "description": "Whether to collapse an inline table written over several lines onto a single line. A comment written anywhere within it keeps it expanded. Defaults to whatever `preferSingleLine` is set to.",
      "type": "boolean",
      "oneOf": [
        {
          "description": "",
          "type": "boolean",
          "enum": [
            true
          ]
        },
        {
          "description": "",
          "type": "boolean",
          "enum": [
            false
          ]
        }
      ]
    },
    "array.spaceSurroundingBrackets": {
      "description": "Whether to write a space inside the brackets of a single-line array.",
      "type": "boolean",
      "oneOf": [
        {
          "description": "ex. `[ 1, 2 ]`",
          "type": "boolean",
          "enum": [
            true
          ]
        },
        {
          "description": "ex. `[1, 2]`",
          "type": "boolean",
          "enum": [
            false
          ]
        }
      ]
    },
    "inlineTable.spaceSurroundingBraces": {
      "description": "Whether to write a space inside the braces of a single-line inline table.",
      "type": "boolean",
      "oneOf": [
        {
          "description": "ex. `{ a = 1 }`",
          "type": "boolean",
          "enum": [
            true
          ]
        },
        {
          "description": "ex. `{a = 1}`",
          "type": "boolean",
          "enum": [
            false
          ]
        }
      ]
    },
    "comment.forceLeadingSpace": {
      "description": "Whether to force a leading space in a comment.",
      "type": "boolean",
      "oneOf": [
        {
          "description": "Adds a leading space if there is none (ex. `# comment`)",
          "type": "boolean",
          "enum": [
            true
          ]
        },
        {
          "description": "Doesn't force a space to be added and maintains what was done (ex. `#comment`)",
          "type": "boolean",
          "enum": [
            false
          ]
        }
      ]
    },
    "cargo.applyConventions": {
      "description": "Whether to apply sorting to a Cargo.toml file.",
      "type": "boolean",
      "oneOf": [
        {
          "description": "",
          "type": "boolean",
          "enum": [
            true
          ]
        },
        {
          "description": "",
          "type": "boolean",
          "enum": [
            false
          ]
        }
      ]
    }
  },
  "properties": {
    "locked": {
      "description": "Whether the configuration is not allowed to be overriden or extended.",
      "type": "boolean"
    },
    "lineWidth": {
      "description": "The width of a line the printer will try to stay under. Note that the printer may exceed this width in certain cases.",
      "type": "number"
    },
    "indentWidth": {
      "description": "The number of characters for an indent.",
      "type": "number"
    },
    "useTabs": {
      "$ref": "#/definitions/useTabs"
    },
    "newLineKind": {
      "$ref": "#/definitions/newLineKind"
    },
    "quoteStyle": {
      "$ref": "#/definitions/quoteStyle"
    },
    "indentTables": {
      "$ref": "#/definitions/indentTables"
    },
    "indentEntries": {
      "$ref": "#/definitions/indentEntries"
    },
    "trailingCommas": {
      "$ref": "#/definitions/trailingCommas"
    },
    "spaceSurroundingEquals": {
      "$ref": "#/definitions/spaceSurroundingEquals"
    },
    "sortKeys": {
      "$ref": "#/definitions/sortKeys"
    },
    "sortArrays": {
      "$ref": "#/definitions/sortArrays"
    },
    "sortInlineTables": {
      "$ref": "#/definitions/sortInlineTables"
    },
    "preferSingleLine": {
      "$ref": "#/definitions/preferSingleLine"
    },
    "array.preferSingleLine": {
      "description": "Whether to collapse an array written over several lines onto a single line when it fits. A comment written within it keeps it expanded. Defaults to whatever `preferSingleLine` is set to.",
      "type": "boolean",
      "oneOf": [
        {
          "description": "",
          "type": "boolean",
          "enum": [
            true
          ]
        },
        {
          "description": "",
          "type": "boolean",
          "enum": [
            false
          ]
        }
      ]
    },
    "inlineTable.preferSingleLine": {
      "$ref": "#/definitions/inlineTable.preferSingleLine"
    },
    "array.spaceSurroundingBrackets": {
      "$ref": "#/definitions/array.spaceSurroundingBrackets"
    },
    "inlineTable.spaceSurroundingBraces": {
      "$ref": "#/definitions/inlineTable.spaceSurroundingBraces"
    },
    "comment.forceLeadingSpace": {
      "$ref": "#/definitions/comment.forceLeadingSpace"
    },
    "cargo.applyConventions": {
      "$ref": "#/definitions/cargo.applyConventions"
    }
  }
}